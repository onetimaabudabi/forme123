# Project Rules

This project is called Forme.

Always keep the existing Apple-inspired UI.

Never redesign screens.

Never introduce mock data.

Always use Firebase Authentication.

Always use Firestore.

Every generated object must persist.

Do not remove existing features.

Prefer reusable components.

Fix bugs automatically if discovered.

Treat this application as production-ready software.